name: CI

# Code CI (test/locks/numerical/bayesian-smoke) intentionally does NOT trigger
# on pull_request_review. Review activity is handled by the separate
# review-gate.yml workflow, which has its own concurrency group, so a review
# submission can no longer cancel an in-progress code CI run for the same PR
# (see docs/spikes/ or PR history for the incident that motivated this split).
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: test (3.11)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: requirements-dev.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt
          python -m pip check

      - name: Check formatting with Ruff
        run: ruff format --check .

      - name: Lint with Ruff
        run: ruff check .

      # Single fast-suite execution collects coverage for both geotestlab and
      # geotestmatch.py in one run; the package gate and the informational
      # app report below both read the same .coverage data rather than
      # re-running the tests a second time.
      - name: Test with coverage (single execution)
        run: pytest --cov=geotestlab --cov=geotestmatch --cov-report=term-missing -q -m "not slow"

      - name: Enforce package coverage gate (geotestlab >= 90%)
        run: coverage report --include="geotestlab/*" --fail-under=90

      - name: Report geotestmatch.py coverage (informational)
        continue-on-error: true
        run: coverage report --include="geotestmatch.py" -m

      - name: Log environment
        if: always()
        run: python -m pip freeze

  locks:
    name: locks (3.11)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: requirements-dev.txt

      - name: Install dependencies
        run: |
          # pip-tools 7.6.0 (pinned in requirements-dev.txt) imports
          # pip._internal.utils.compat.stdlib_pkgs, which pip >=26.2 removed.
          # Pin pip below that so the lock check keeps reproducing the
          # committed locks instead of failing on tooling drift.
          python -m pip install --upgrade "pip<26.2"
          python -m pip install -r requirements-dev.txt
          python -m pip check

      - name: Verify lock reproducibility
        run: python scripts/compile_requirements.py --check --diagnostics-dir lock-diagnostics

      - name: Upload lock diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: lock-diagnostics
          path: lock-diagnostics/
          if-no-files-found: ignore

  numerical:
    name: numerical (3.11)
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: requirements-dev.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt
          python -m pip check

      - name: Run numerical golden regressions
        run: pytest tests/test_numerical_characterisation.py -q -m slow

  bayesian-smoke:
    name: bayesian-smoke (3.11)
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: requirements-dev.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt
          python -m pip check

      # Real reduced-sampling PyMC sampling path (draws=20/tune=10/chains=1).
      # Treat this as an execution-path smoke test, not evidence of production
      # MCMC convergence. The app-level trace-creation AppTest exercises the
      # full UI path (match -> evaluate -> Bayesian run) with the same tiny
      # sampling profile.
      - name: Run reduced-sampling Bayesian smoke
        run: |
          pytest tests/test_bayesian_core.py::TestReducedSamplingSmoke -q
          pytest tests/test_bayesian_state_app.py::TestBayesianTraceCreation -q
